# v1.0.8 Post-Release Compatibility Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复机械渡鸦附魔台在附魔师珍珠生效时错误显示零青金石选项不可用，以及移除 Easy Magic 后其持久化槽位物品无法取回的问题，并覆盖更新现有 `v1.0.8` Release。

**Architecture:** 机械菜单在现有 `getGoldCount()` 入口提供只用于显示和菜单判定的珍珠虚拟数量。Apotheosis 菜单 Mixin 在服务端构造时记录待迁移的 Easy Magic 库存，并在首次 `broadcastChanges()` 时迁移，避开 Raven 子类尚未完成构造时触发虚方法；迁移只回收历史物品，不在 Easy Magic 缺席时继续提供其功能。

**Tech Stack:** Java 17、Minecraft Forge 1.20.1、Apotheosis、Sponge Mixin、JUnit 5、Gradle 8.8、GitHub CLI

## Global Constraints

- `mod_version` 保持 `1.0.8`。
- Enigmatic Legacy 与 Easy Magic 均保持可选兼容，不新增编译或运行依赖。
- 附魔师珍珠只免除玩家手动附魔的青金石，不改变经验消耗，不生成青金石，不影响机械自动附魔。
- Easy Magic 未安装时只迁移历史库存，不启用持久化槽位、刷新功能或宽松书架功能。
- 不改变 Apotheosis 的永恒、量子化、阿卡那、宝藏、线索、黑名单、随机种子和事件流程。
- 输出 JAR 只放入 `C:\Users\Lenovo\Desktop\Ai_Run\output`，不部署到 `mods`。
- 发布时覆盖现有 `v1.0.8` 标签和同名 Release 资产，不创建新版本号。

---

### Task 1: 修复机械渡鸦的附魔师珍珠青金石显示

**Files:**
- Modify: `src/test/java/com/apotheosis_artifice/enchant/EnchanterPearlCompatibilityTest.java`
- Modify: `src/main/java/com/apotheosis_artifice/enchant/MechanicalRavenEnchantMenu.java`

**Interfaces:**
- Consumes: `EnigmaticLegacyCompat.isEnchanterPearlActive(Player)`、继承自 `ApothEnchantmentMenu` 的 `player`。
- Produces: `MechanicalRavenEnchantMenu.getGoldCount()` 在珍珠生效时返回虚拟显示值 `64`。

- [ ] **Step 1: 写失败回归测试**

把 `playerOperatedMenusApplyTreasureStatsWithoutReplacingEnchanting` 中对机械菜单不得引用兼容类的旧断言改为只禁止合并附魔，并添加：

```java
@Test
void mechanicalRavenShowsPearlEnchantmentsAsAvailableWithoutLapis() throws IOException {
    String mechanicalMenu = read("enchant", "MechanicalRavenEnchantMenu.java");

    int method = mechanicalMenu.indexOf("public int getGoldCount()");
    int pearlCheck = mechanicalMenu.indexOf(
        "EnigmaticLegacyCompat.isEnchanterPearlActive(this.player)", method);
    int fuelRead = mechanicalMenu.indexOf("this.tile.getFuelInv()", method);
    assertTrue(method >= 0);
    assertTrue(pearlCheck > method);
    assertTrue(fuelRead > pearlCheck);
    assertTrue(mechanicalMenu.substring(pearlCheck, fuelRead).contains("return 64;"));
}
```

- [ ] **Step 2: 运行测试并确认旧实现失败**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test --tests com.apotheosis_artifice.enchant.EnchanterPearlCompatibilityTest.mechanicalRavenShowsPearlEnchantmentsAsAvailableWithoutLapis
```

Expected: `FAILED`，因为 `MechanicalRavenEnchantMenu` 尚未在读取真实燃料前检查珍珠。

- [ ] **Step 3: 实现最小显示修复**

在 `MechanicalRavenEnchantMenu` 导入 `EnigmaticLegacyCompat`，并把方法开头改为：

```java
@Override
public int getGoldCount() {
    if (EnigmaticLegacyCompat.isEnchanterPearlActive(this.player)) return 64;
    if (this.tile != null) {
        lastGoldCount = this.tile.getFuelInv().getStackInSlot(0).getCount();
        return lastGoldCount;
    }
    int v = this.getSlot(1).getItem().getCount();
    if (v > 0) lastGoldCount = v;
    return lastGoldCount;
}
```

- [ ] **Step 4: 运行定向测试和完整兼容测试**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test --tests com.apotheosis_artifice.enchant.EnchanterPearlCompatibilityTest.mechanicalRavenShowsPearlEnchantmentsAsAvailableWithoutLapis
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test --tests com.apotheosis_artifice.enchant.EnchanterPearlCompatibilityTest
```

Expected: 两次均为 `BUILD SUCCESSFUL`。

- [ ] **Step 5: 提交机械菜单修复**

```powershell
git add -- src/test/java/com/apotheosis_artifice/enchant/EnchanterPearlCompatibilityTest.java src/main/java/com/apotheosis_artifice/enchant/MechanicalRavenEnchantMenu.java
git commit -m "fix: show pearl enchanting as available"
```

---

### Task 2: 移除 Easy Magic 后迁移历史持久化物品

**Files:**
- Modify: `src/test/java/com/apotheosis_artifice/enchant/EnchanterPearlCompatibilityTest.java`
- Modify: `src/main/java/com/apotheosis_artifice/mixin/ApothEnchantmentMenuMixin.java`
- Modify: `src/main/java/com/apotheosis_artifice/enchant/MechanicalRavenEnchantMenu.java`

**Interfaces:**
- Consumes: `EasyMagicEnchantingStorage.getEasyMagicInventory()`、`Inventory.placeItemBackInInventory(ItemStack)`、`AbstractContainerMenu.broadcastChanges()`。
- Produces: `artifice$queueEasyMagicInventoryMigration(...)`、`artifice$migrateEasyMagicInventory()`、`artifice$moveOrReturnEasyMagicStack(Container, int, int)`。

- [ ] **Step 1: 写失败回归测试**

在 `EnchanterPearlCompatibilityTest` 添加：

```java
@Test
void removingEasyMagicMigratesPersistentItemsAfterMenuConstruction() throws IOException {
    String menuMixin = read("mixin", "ApothEnchantmentMenuMixin.java");
    String mechanicalMenu = read("enchant", "MechanicalRavenEnchantMenu.java");

    assertTrue(menuMixin.contains("artifice$pendingEasyMagicInventory"));
    assertTrue(menuMixin.contains("@Inject(method = \"broadcastChanges\", at = @At(\"HEAD\"))"));
    assertTrue(menuMixin.contains("if (EasyMagicCompat.isLoaded())"));
    assertTrue(menuMixin.contains("this.artifice$moveOrReturnEasyMagicStack(source, 0, 0)"));
    assertTrue(menuMixin.contains("this.artifice$moveOrReturnEasyMagicStack(source, 1, 1)"));
    assertTrue(menuMixin.contains("this.artifice$returnEasyMagicStack(source, 2)"));
    assertTrue(menuMixin.contains("target.mayPlace(stack)"));
    assertTrue(menuMixin.contains("placeItemBackInInventory(stack)"));
    assertTrue(menuMixin.contains("source.setItem(sourceSlot, ItemStack.EMPTY)"));
    assertTrue(mechanicalMenu.contains("pendingEasyMagicInput"));
    assertTrue(mechanicalMenu.contains("placeItemBackInInventory(fromSave.copy())"));
}
```

- [ ] **Step 2: 运行测试并确认旧实现失败**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test --tests com.apotheosis_artifice.enchant.EnchanterPearlCompatibilityTest.removingEasyMagicMigratesPersistentItemsAfterMenuConstruction
```

Expected: `FAILED`，首个断言指出不存在待迁移库存。

- [ ] **Step 3: 在服务端构造时登记迁移，在首次广播时执行**

在 `ApothEnchantmentMenuMixin` 增加字段：

```java
@Unique private Inventory artifice$playerInventory;
@Unique private Container artifice$pendingEasyMagicInventory;
```

让 `artifice$rememberPlayer` 同时保存 `Inventory`，并把服务端构造注入改为：

```java
private void artifice$rememberPlayer(Inventory inventory) {
    this.artifice$menuPlayer = inventory.player;
    this.artifice$playerInventory = inventory;
}

@Inject(method = "<init>(ILnet/minecraft/world/entity/player/Inventory;Lnet/minecraft/world/inventory/ContainerLevelAccess;Ldev/shadowsoffire/apotheosis/ench/table/ApothEnchantTile;)V", at = @At("TAIL"), remap = false)
private void artifice$bindServerEasyMagicInventory(int id, Inventory inventory, ContainerLevelAccess access, ApothEnchantTile tile, CallbackInfo ci) {
    this.artifice$rememberPlayer(inventory);
    if (!(tile instanceof EasyMagicEnchantingStorage storage)) return;
    if (EasyMagicCompat.isLoaded()) {
        this.artifice$bindEasyMagicInventory(storage.getEasyMagicInventory());
    } else {
        this.artifice$pendingEasyMagicInventory = storage.getEasyMagicInventory();
    }
}
```

迁移延迟到首次广播，确保 Raven 与机械 Raven 的字段均已初始化：

```java
@Inject(method = "broadcastChanges", at = @At("HEAD"))
private void artifice$migrateEasyMagicInventory(CallbackInfo ci) {
    Container source = this.artifice$pendingEasyMagicInventory;
    if (source == null) return;
    this.artifice$pendingEasyMagicInventory = null;
    this.artifice$moveOrReturnEasyMagicStack(source, 0, 0);
    this.artifice$moveOrReturnEasyMagicStack(source, 1, 1);
    this.artifice$returnEasyMagicStack(source, 2);
}

@Unique
private void artifice$moveOrReturnEasyMagicStack(Container source, int sourceSlot, int targetSlot) {
    ItemStack stack = source.getItem(sourceSlot).copy();
    if (stack.isEmpty()) return;
    Slot target = this.slots.get(targetSlot);
    if (!target.hasItem() && target.mayPlace(stack)) {
        target.set(stack);
    } else {
        this.artifice$playerInventory.placeItemBackInInventory(stack);
    }
    source.setItem(sourceSlot, ItemStack.EMPTY);
}

@Unique
private void artifice$returnEasyMagicStack(Container source, int sourceSlot) {
    ItemStack stack = source.getItem(sourceSlot).copy();
    if (stack.isEmpty()) return;
    this.artifice$playerInventory.placeItemBackInInventory(stack);
    source.setItem(sourceSlot, ItemStack.EMPTY);
}
```

- [ ] **Step 4: 防止机械旧保存槽覆盖待迁移输入**

在 `MechanicalRavenEnchantMenu` 的服务端构造器中，将非 Easy Magic 分支改为：

```java
} else if (!EasyMagicCompat.isLoaded()) {
    boolean pendingEasyMagicInput = te instanceof EasyMagicEnchantingStorage storage
        && !storage.getEasyMagicInventory().getItem(0).isEmpty();
    if (pendingEasyMagicInput || !this.enchantSlots.getItem(0).isEmpty()) {
        inv.placeItemBackInInventory(fromSave.copy());
        te.setSavedEnchantSlot(ItemStack.EMPTY);
    } else {
        this.enchantSlots.setItem(0, fromSave.copy());
    }
}
```

并导入 `EasyMagicEnchantingStorage`。Easy Magic 历史输入优先迁入普通槽，机械旧保存值安全归还玩家；没有历史输入时保持现有机械恢复行为。

- [ ] **Step 5: 运行定向测试和全部测试**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test --tests com.apotheosis_artifice.enchant.EnchanterPearlCompatibilityTest.removingEasyMagicMigratesPersistentItemsAfterMenuConstruction
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain test
```

Expected: 两次均为 `BUILD SUCCESSFUL`，全部测试零失败。

- [ ] **Step 6: 检查差异并提交迁移修复**

Run:

```powershell
git diff --check
git diff -- src/main/java src/test/java
```

Expected: `git diff --check` 无输出；差异只包含本计划要求的兼容修复和测试。

Commit:

```powershell
git add -- src/test/java/com/apotheosis_artifice/enchant/EnchanterPearlCompatibilityTest.java src/main/java/com/apotheosis_artifice/mixin/ApothEnchantmentMenuMixin.java src/main/java/com/apotheosis_artifice/enchant/MechanicalRavenEnchantMenu.java
git commit -m "fix: recover Easy Magic inventory when removed"
```

---

### Task 3: 完整验证并覆盖发布 v1.0.8

**Files:**
- Verify: `gradle.properties`
- Build: `build/libs/apotheosis_artifice-1.0.8.jar`
- Output: `C:\Users\Lenovo\Desktop\Ai_Run\output\apotheosis_artifice-1.0.8.jar`

**Interfaces:**
- Consumes: Gradle Java 17 构建、Git 远端 `origin`、GitHub Release `v1.0.8`。
- Produces: 与新 `master` 和新 `v1.0.8` 标签一致的 Release JAR 及 SHA-256。

- [ ] **Step 1: 核对版本与可选依赖声明**

Run:

```powershell
Select-String -Path gradle.properties -Pattern '^mod_version=1.0.8$'
rg -n "mandatory=true|modId=\"(easymagic|enigmaticlegacy)\"" src/main/resources/META-INF/mods.toml
```

Expected: 版本严格为 `1.0.8`；Easy Magic 与 Enigmatic Legacy 不作为强制依赖出现。

- [ ] **Step 2: 使用 Java 17 进行全新完整构建**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\java.exe' -classpath 'gradle\wrapper\gradle-wrapper.jar' org.gradle.wrapper.GradleWrapperMain clean build
git diff --check
```

Expected: `BUILD SUCCESSFUL`、全部测试通过，`git diff --check` 无输出。

- [ ] **Step 3: 检查 JAR 内容与依赖边界**

Run:

```powershell
& 'D:\JAVA\JAVA_17\bin\jar.exe' tf 'build\libs\apotheosis_artifice-1.0.8.jar'
& 'D:\JAVA\JAVA_17\bin\jdeps.exe' -verbose:class 'build\libs\apotheosis_artifice-1.0.8.jar'
```

Expected: JAR 包含本项目类、Mixin 配置和资源；`jdeps` 输出中没有 Easy Magic 或 Enigmatic Legacy 外部类的直接链接。

- [ ] **Step 4: 复制正式产物并计算哈希**

Run:

```powershell
Copy-Item -LiteralPath 'build\libs\apotheosis_artifice-1.0.8.jar' -Destination 'C:\Users\Lenovo\Desktop\Ai_Run\output\apotheosis_artifice-1.0.8.jar' -Force
Get-FileHash -Algorithm SHA256 'build\libs\apotheosis_artifice-1.0.8.jar'
Get-FileHash -Algorithm SHA256 'C:\Users\Lenovo\Desktop\Ai_Run\output\apotheosis_artifice-1.0.8.jar'
```

Expected: 两个 SHA-256 完全一致。

- [ ] **Step 5: 推送 master 并强制移动现有 v1.0.8 标签**

Run:

```powershell
git push origin master
git tag -f v1.0.8 HEAD
git push origin --force refs/tags/v1.0.8
```

Expected: 远端 `master` 与 `v1.0.8` 均指向当前 `HEAD`。

- [ ] **Step 6: 覆盖 Release 资产并更新说明**

Run:

```powershell
gh release upload v1.0.8 'C:\Users\Lenovo\Desktop\Ai_Run\output\apotheosis_artifice-1.0.8.jar' --clobber
gh release edit v1.0.8 --target master --notes "v1.0.8 compatibility re-release: fixes Mechanical Raven Enchanter Pearl no-lapis availability display and safely recovers persistent Easy Magic enchanting inventory after Easy Magic is removed. Easy Magic and Enigmatic Legacy remain optional compatibility targets."
```

Expected: 同名资产被替换，Release 保持非草稿、非预发布。

- [ ] **Step 7: 最终远端一致性核验**

Run:

```powershell
git ls-remote origin refs/heads/master refs/tags/v1.0.8
gh release view v1.0.8 --json url,targetCommitish,isDraft,isPrerelease,tagName,assets
git status --short --branch
```

Expected: 远端 `master` 与标签提交等于本地 `HEAD`；Release 为公开正式版；资产 digest 等于本地 SHA-256；工作树干净。
